---
paths:
  - "crates/openlogi-desktop/locales/**"
  - "crates/openlogi-desktop/src/i18n.rs"
  - ".github/scripts/i18n/**"
  - ".github/workflows/crowdin.yml"
---

# i18n (rust-i18n + Crowdin)

- **`locales/en.yml` is the English source of truth** (the English text IS the
  key). When adding or changing copy, add the **same key to every**
  `locales/<code>.yml` in the same change.
- Values for non-English locales may be a best-effort translation (or temporarily
  match English when no better string is ready). Crowdin improves them later;
  do not leave keys missing — the parity test fails the build.
- YAML is parsed at **compile time** — a syntax error fails the entire GUI
  build. Quote values containing `: ` or a trailing `:`.
- The locale key test (`i18n.rs`, `locale_files_have_the_same_keys`) requires
  every non-English file to carry **exactly** the keys in `en.yml` (no lag, no
  extras).
- Adding a locale: drop the `.yml` in `locales/` and add the `SUPPORTED` entry in
  `i18n.rs` (picker order: native-name alphabetical per script). Keep the
  include list in the parity test in sync.
- Check with `cargo test -p openlogi-desktop i18n`. CI's Linux tests exclude the GUI,
  so this runs on macOS CI and locally (needs the Xcode/Metal env from devenv).
- Crowdin workflow (`crowdin.yml` + `.github/workflows/crowdin.yml`):
  1. Snapshot complete `locales/*.yml`
  2. Upload `en.yml` sources + per-language translations from git
  3. Download with `skip_untranslated_strings` (sparse export is OK)
  4. **Merge** via `.github/scripts/i18n/merge_crowdin_download.py` — only apply values
     that differ from English; never delete keys; never keep English fill-in
  5. Open `crowdin/i18n` only when a real value improved
